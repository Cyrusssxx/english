# 最近改动总结

> 维护自 2026-08-26，记录本项目较大改动。详细逐条见 `CHANGELOG.md`。

## 2026-09-04 修复：英语一阅读净化正文混题 + 补全 2021_text2

- **做了什么**：① 剔除 EN1 各篇 sentences 里误入的题目句/选项/题号/页标（2024 全 4 篇 + 2011_text3 + 2014_text3），只留纯正文；② 重建 **en1_2021_text2** 缺失正文（从英一 2021 PDF 提取，8 段 23 句，含逐句中译），原为 0 句；③ 同步 `en1_index.json` sentence_count（7 处）。
- **文件**：`pwa/data/en1/{2011,2014,2021,2024}.json` + `pwa/data/en1_index.json` + `pwa/sw.js`。
- **验证**：断言近十年(2015-2025)全部文章无正文缺失/无题目混入、题目完整。SW `en2-4d040940`。
- **遗留**：2011_text3 题目 questions=0（较远年份，需从 2011 解析补题）。

## 2026-09-03 微调：中频/别误杀标题置顶 + conceited 归位

- **做了什么**：多栏里让「零中选·中频」大标题、「别误杀 · 这些反而常中选」子标题开新栏顶到栏首（仅这两个）；低频组删掉 conceited 那条（归回感情色彩太过主分类），低频 14→13。
- **文件**：`pwa/js/mindmap.js`、`pwa/mindmap.html`（.att-title-top/.att-sub-title-top CSS）、`pwa/data/mindmap.json`、`pwa/sw.js`。SW `en2-69b4c3ef`。

## 2026-09-03 改版：态度词改 CSS 多栏自动填满

- **做了什么**：态度词从两排 grid 改成 CSS 多栏流（`.att-flow`, column-width:300px 自动分栏），5 分支作大标题、子分组作次标题、46 条流式铺栏自动占满不留右侧空白；防跨栏截断、窄屏收单栏。reading 等仍 mm-cols 横排。
- **文件**：`pwa/js/mindmap.js`（renderAttitude 分派）、`pwa/mindmap.html`（.att-flow CSS，删旧 grid）。json 未动。
- **验证**：jsdom 确认 attitude 多栏 5 标题+4 子标题+46 条、reading 仍 4 列。SW `en2-a540762f`。

## 2026-09-03 回退：撤掉态度词「单张大卡」，回到两排 grid

- 用户对 `b7e8d96`（单张纵向大卡）不满意，revert（`182c99d`）回 `b8aaa5a` 两排 grid 形态（第一排零中选两列 + 第二排其余，低频在铁证卡内拆两列垫底）。
- `renderAttitude`/`.mm-single` 已清除；SW `en2-9f8cebff`。

## 2026-09-03 调整：低频并入铁证卡内两列 + 顶部对齐

- **做了什么**：低频 14 条太长，从右侧「中频+低频」列移出、并入左侧「零中选·铁证」列卡内作为第三个子分组（加 `full:true`）；铁证卡内部「铁证 | 别误杀」上半并排、低频带分隔线拆两列垫在下方。右侧列改名「零中选·中频」只剩中频 3 条。各卡片顶部对齐（grid align-items:start）。
- **新增能力**：`renderSplitItems` 支持 `full:true` 子项占整行、内部再拆两列（`.mm-full{grid-column:1/-1}` + 内嵌 `.mm-split`）。
- **文件**：`pwa/js/mindmap.js`（数据 + full 渲染）、`pwa/mindmap.html`（.mm-full CSS）。json 已同步。
- **验证**：jsdom 确认 列4 铁证+低频(27, 三子分组)、列3 中频(3)、全图 46 条无丢失。SW `en2-9f8cebff`。

## 2026-09-03 调整：态度词改为两排 grid 布局

- **做了什么**：态度词从横向单排改成上下两排：第一排 = 零中选·铁证 + 零中选·中频+低频；第二排 = 不符合价值观 + 感情色彩太过 + 模糊的。每排最多并排 4 列、不够留空。
- **关键修复**：`.mm-cols-attitude` grid CSS 原是 4 列版（c1-c4 areas），attitude 现 5 列导致第 5 个「模糊的」无 grid-area、落入自动布局区，真实排布是乱的。重写为 `repeat(4,minmax(0,1fr))` + areas `"g1 g1 g2 g2" / "g3 g4 g5 ."` + 5 个 nth-child 映射。
- **文件**：`pwa/mindmap.html`（grid CSS + 提示文案）。`pwa/js/mindmap.js` 未动（DOM 顺序本就符合映射）。
- **验证**：jsdom 确认 attitude 5 列 DOM 序 = 不符合价值观/感情色彩太过/中频+低频/铁证(split)/模糊的，与 nth-child 区域映射逐一对应；阅读一张纸仍普通 flex 横滑。
- **教训**：`mm-cols-attitude` 每次增减列数必须同步重写 `grid-template-areas` + `nth-child` 映射，否则第 5+ 个元素落到 grid 自动区、真实布局错乱（jsdom 测不出，只能静态推演）。SW `en2-5723c998`。

## 2026-09-02 调整：态度词「真题零中选」拆两半分两列

- **做了什么**：态度词原第 4 栏「真题零中选」(30 条 split 双列)拆成两半、分两列：
  - **零中选·中频+低频**（中频 3 条 + 低频 14 条 = 17 条）→ 紧跟「感情色彩太过」(含"自负的"那栏) 之后，单独成列。
  - **零中选·铁证**（铁证 7 条 + 别误杀 6 条 = 13 条，仍 split 双列）→ 紧随其后，两半相邻便于对比。
  - 「模糊的」殿后。态度词现 5 列。
- **文件**：`pwa/js/mindmap.js`（attitude branches 4→5 列）、`pwa/mindmap.html`（列数提示文案）、`pwa/data/mindmap.json`（由内联 DATA 反向重新生成同步）。
- **验证**：jsdom 确认 5 列、中频低频半紧跟感情色彩太过、全图 46 条目无丢失。SW `en2-fbbc1ef4`。
- **注**：尝试 agent-browser 真实截图复核失败（Chromium install 在本机反复 SIGTERM），以 jsdom 为准。

## 2026-09-02 态度词墓碑加第 4 栏「真题零中选」+ 回退笔记功能

### 回退笔记功能
- 用户反馈笔记功能「做得差了」。`e2213d5`（富文本笔记）已 revert（`c1cefb8`）并推送；方法导图恢复「高亮 + 卡片」形态，笔记相关代码零残留。**高亮功能保留**。

### 态度词小墓碑新增第 4 栏：真题零中选（实测统计）
- **做了什么**：从本库真题里自动统计态度题，得出「绝对不可能选的态度词」清单，作为态度词图的第 4 栏。
- **统计口径**：扫描英二 2007–2025 + 英一全部年份，识别态度题 → 清洗选项（截 PDF 污染、只留 ≤3 词形容词）→ 词形归一 → 统计「出现次数 / 中选次数」。**有效样本 28 道**（英二 13 + 英一 15）。
- **核心结论**：
  - **tolerant 11 次出现、0 次中选** —— 头号陷阱词，铁证。
  - 高频零中选：appreciation 4/0、indifferent 4/0、ambiguous 3/0、contempt 3/0、puzzled 3/0、uncertain 3/0。
  - 中频：biased 2/0、respectful 2/0、scornful 2/0。
  - **反直觉点**：appreciation / respectful / impartial / satisfaction / understanding 这些「好听」的词同样 0 中选。
  - 另附「别误杀」：skeptical 8 中 3、critical 7 中 3、supportive 6 中 3、disapproval 2 中 2、doubtful 3 中 2、approval 3 中 2。
- **文件**：`pwa/js/mindmap.js`（第 4 栏数据 + intro 口径说明）、`pwa/mindmap.html`（列数提示）、`pwa/data/mindmap.json`（由内联 DATA 反向重新生成，严格同步）。
- **验证**：jsdom 实跑确认态度词 4 列、第 4 栏 4 分组（7/3/14/6 条）、全页 162 条目正常渲染。SW `en2-c6ea3065`。

## 2026-08-26 新增：方法导图支持条目高亮标记

- **做了什么**：方法导图每个条目可点击高亮（黄色标记），再点取消；用 localStorage 持久化（键 `mm_highlights_v1`），刷新后仍保留；页面顶部加「清除高亮」按钮。深浅色主题均适配。
- **文件**：`pwa/js/mindmap.js`（高亮逻辑 + 清除函数）、`pwa/mindmap.html`（工具条 + 高亮样式）。
- **修复**：`saveHL` 原用 `[].slice.call(hlSet)` 对 Set 取不到值导致持久化失效，改为 `Array.from(hlSet)`。
- **验证**：jsdom 实跑确认点击切换/多选持久化/清除/刷新恢复均正确。SW `en2-bfd262b2`。

## 2026-08-26 微调：方法导图「题型」列拆双列各半

- **需求**：阅读一张纸「题型」列（6 题型×4 行）单列太高，要求拆成两列各一半。
- **改动**：`mindmap.js` 题型分支加 `split:2`，新增 `renderSplitItems()`（子项直接进 2 列网格，不被 `.branch-body` 整包）；`mindmap.html` 加 `.mm-col-split{flex:2 1 0;min-width:540px}` 与 `.mm-split{grid 2列}`，更新提示文案。
- **验证**：jsdom 7 列不变、题型列 6 题型块落入双列网格、每块 4 行、其余列不变。
- **提交**：（本次）；SW `CACHE_VER=en2-f07219a9`。

## 2026-08-26 新增：单词本导出 TXT

- **需求**：单词本支持导出 `.txt`，一行一词、无序号、`单词 意思` 排列。
- **改动**：`pwa/vocab.html` 工具栏加「导出TXT」按钮（`__all__` 与单本均有），新增 `exportDeckTxt()` 取当前视图 `curRows` 逐行输出 `word meaning`、加 BOM、空释义仅留单词。
- **验证**：jsdom 实跑真实函数，格式正确、无序号、BOM 存在。
- **提交**：（本次）；SW `CACHE_VER=en2-9aa45279`。

## 2026-08-26 改版：方法导图改为全屏多列宽表

- **需求**：用户选全屏多列宽表——去 Tab，每模块一整列横向并排铺满屏，整页可横滚。
- **改动**：`mindmap.html` 去 `#mmTabs`、容器全屏，加 `.mm-cols/.mm-col/.mm-col-head` + 标题/横滑提示；`mindmap.js` 的 `renderMap` 改多列、`init` 去 Tab 直接渲染全部图。
- **结构**：态度词 3 列 / 阅读一张纸 4 列；列内递归 `renderItems`。
- **验证**：jsdom 2 图 7 列、列头正确、67 叶子+14 子分组，旧结构零残留。
- **提交**：（本次）；SW `CACHE_VER=en2-418fa2b3`。

## 2026-08-26 修复：方法导图线上不更新（SW 缓存顶旧版）

- **现象**：静态页推送后线上仍显示旧 SVG 版。
- **根因**：`storage.js` 注册 SW 未加 `updateViaCache`、未监听 `controllerchange`，新 SW 激活后当前页仍用旧缓存。
- **修复**：注册加 `updateViaCache:'none'` + `controllerchange` 自动 `location.reload()`（防循环）。今后推送刷新一次即生效。
- **文件**：`pwa/js/storage.js`、`pwa/sw.js`（CACHE_VER=en2-b12e8ccc）。
- **用户侧**：旧 SW 需清一次（DevTools 卸载或开无痕窗口）再刷新，之后自动。

## 2026-08-26 重构：方法导图改为静态方法页（弃用 SVG 交互导图）

- **为什么改**：用户反馈 SVG 交互导图（缩放/拖拽/折叠）"不方便阅读"——默认字号小、需手动缩放拖拽、结构不直观。
- **现在是什么**：保留 Tab 切换两张图，内容改为**静态卡片 + 分组排版**——打开即读、自然滚动、字号大、层级分明，去掉所有缩放/拖拽/折叠。
- **文件改动**：
  - `pwa/js/mindmap.js` 整体重写：数据**内联**（不再 fetch），零网络依赖、离线可用；纯 DOM 卡片渲染（色条标题 + 行 + 子分组），英文加粗；Tab 仅切面板显隐。
  - `pwa/mindmap.html` 主体重写：去掉工具栏/SVG/树形兜底，改为 `.mm-page` + `#mmTabs` + `#mmContent`。
  - `pwa/data/mindmap.json` 保留为可读源（不再被读取）。
- **验证**：jsdom 实跑两图正常渲染（态度词 3 卡 16 行 / 阅读 4 卡 51 行 14 子分组），无空白、无 SVG、无 fetch。
- **入口**：全部 7 个页面导航栏「方法导图」不变。
- **提交**：（本次）；SW `CACHE_VER=en2-f1eaa521`。
- **注意**：数据已内联，现在 `file://` 直接双击打开也能正常显示（不再依赖 http / fetch）。

## 2026-08-26 新增「方法导图」页面

- **做了什么**：把两张考研阅读手绘思维导图（态度词小墓碑、阅读一张纸）做成网页版可交互思维导图。
- **文件**：
  - 新增 `pwa/data/mindmap.json`（两张导图数据，分支配色）
  - 新增 `pwa/mindmap.html` + `pwa/js/mindmap.js`（SVG 渲染引擎）
  - 改动 `pwa/index.html` 等 6 个页面 + `tools/build_sw.py`
- **能力**：桌面端滚轮缩放、拖拽平移、点节点折叠/展开、Tab 切换两张图；窄屏(<768px)自动退化成树形列表。
- **入口**：全部 7 个页面导航栏新增「方法导图」。
- **提交**：`4e565e8`（已 push，SW `CACHE_VER=en2-e0e75cc2`）。
- **注意**：该 SVG 方案已被 2026-08-26 重构为静态方法页，下方热修/二次调整均针对旧 SVG 方案，可忽略。

### 2026-08-26 热修：方法导图空白
- **现象**：页面只显示导航/Tab/工具栏，导图区域空白。
- **根因**：`mindmap.js` 的 `buildTree` 创建的 `root` 漏设 `children`（`layout` 把 root 当叶子，只画根节点）。
- **修复**：`root` 加 `children: map.branches`。jsdom 实跑验证整树渲染（态度词图 20 节点）。
- **提交**：`3e5ce5e`（已 push，SW `CACHE_VER=en2-cd8d897e`）。

### 2026-08-26 二次调整：竖向排列 + 默认放大 + 消除重叠(串)
- **改动**：`mindmap.js` 布局由横向改为**竖向 top-down**（root 在顶、分支自上而下展开）；默认放大 `DEFAULT_ZOOM=1.32`（原 fit 缩到 ~0.8 倍字号太小）；兄弟节点按**实际宽度累加间距**消除同行重叠（态度词 4 处/阅读图 2 处"串"已清零）。
- **验证**：jsdom 实跑两图均 0 重叠、边竖向、默认 `scale(1.32)`（态度词 20 节点 / 阅读一张纸 70 节点）。
- **提交**：`a4b2930`（已 push，SW `CACHE_VER=en2-d018ad93`）。

## 2026-08-23 英语一(en1) 全 16 年数据全量修复

- 64 篇/1180 句/299 题的答案、解析、题干中文、选项中文、逐句 cn、ref_cn 全部清零（仅 4 题关联句为算法极限）。
- en1 与英语二体验已一致。提交 `ccf0a90` / `438dc32`。

## 2026-08-20 修复首页加载卡死

- SW 不阻塞 + 文件加载超时提示。详见 CHANGELOG。
