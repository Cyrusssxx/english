# 考研英语二真题精翻 PWA · 开发全流程总结

> 一个完全离线、纯前端、可安装的考研英语（二）历年真题精读学习应用。
> 从「AI 编造题库」推倒重做，到覆盖 2010–2025 全 16 年真题、内置词典/词组/多词书/间隔重复背单词/批注系统的完整学习工具。
>
> - **在线地址（GitHub Pages）**：仓库 `github.com/Cyrusssxx/english`，`pwa/` 目录即站点根
> - **当前版本**：`CACHE_VER = en2-v33`（Service Worker 缓存版本）
> - **文档生成时间**：2026-07-27

---

## 一、项目概述

### 1.1 是什么

面向考研英语（二）的**真题精读 + 背单词一体化学习 PWA**，核心能力：

- **逐句精读**：每道阅读真题按句拆分，英文原文 + 官方解析译文逐句对照，点句即展开中文。
- **真题做题**：完整题干/选项/答案/解析，支持作答判分、定位原文依据、清除重做（二刷）。
- **离线词典**：正文任意难词/词组可点查释义（ECDICT 词库 + 真题词组库），带音标、真题考频徽标。
- **多词书生词本**：一个词可归属多本词书，支持内置词书导入、自建词书、导入自定义 JSON 词书。
- **背单词卡片**：以词书为队列的间隔重复（SRS）背诵，三种学习模式、进度续存、自然美声发音、自定义快捷键。
- **手动批注**：正文可打荧光高亮、整句块高亮、写文字批注（支持贴图），本地存储 + 导入导出备份。
- **完全离线**：Service Worker 预缓存全部资源，安装后断网可用；可添加到主屏当原生 App 用。

### 1.2 覆盖范围

| 维度 | 数量 |
|---|---|
| 真题年份 | 2010–2025，共 **16 年** |
| 阅读文章 | 每年 Text 1–4，共 **64 篇** |
| 阅读题目 | 共 **320 题** |
| 逐句译文 | 全部改用**官方解析译文**（非 AI 意译） |
| 内置词书 | 真题核心/考纲乱序/形近易混/真题词组/唐迟词组/熟词僻义/真题高频较难词 等 |
| 离线词典 | 1731 难词 + 词形变形（ECDICT 抽取） |
| 真题考频索引 | 6473 键（单词/词组在 16 年真题中的出现次数） |

---

## 二、技术栈与架构

### 2.1 技术选型

- **前端**：原生 HTML + CSS + JavaScript（**零框架、零构建**），多页应用（MPA）。
- **存储**：`localStorage`（配置/词书元数据/批注文本）+ `IndexedDB`（生词记录/批注贴图）。
- **离线**：Service Worker（`sw.js`）预缓存全部页面/样式/脚本/题库/词典/图标。
- **PWA**：`manifest.webmanifest` + 图标，`display: standalone`，可安装到主屏。
- **数据生产（离线工具链，不进运行时）**：Python + PyMuPDF（PDF 文本/渲染）+ RapidOCR（扫描图识别）+ ECDICT 词库。

> 设计原则：**运行时纯静态**，所有繁重的数据加工（PDF 解析、OCR、词典构建、考频统计）都在本地用 Python 工具离线完成，产出静态 JSON，浏览器只读不算。

### 2.2 运行时模块（`pwa/`）

| 文件 | 行数 | 职责 |
|---|---|---|
| `index.html` | — | 首页，年份/文章列表入口 |
| `article.html` + `js/article.js` | 626 | 精读页：逐句翻译、做题判分、词典弹卡、词组高亮、批注宿主 |
| `study.html` + `js/study.js` | 524 | 背单词卡片：SRS 队列、三模式、续存、发音、快捷键 |
| `vocab.html` | — | 单词本：多词书管理、导入、内置词书、分页渲染、重学本书 |
| `favorites.html` | — | 句子收藏 |
| `js/storage.js` | 411 | 数据层：IndexedDB 生词、词书元数据、SRS、统计、迁移 |
| `js/dict.js` | 127 | 离线词典/词组/考频加载与查询（词形还原） |
| `js/annotate.js` | 403 | 批注系统：荧光/块高亮/文字批注/贴图，存储与导入导出 |
| `js/common.js` | 67 | 公共工具（导航、暗色模式等） |
| `css/style.css` | — | 全站样式（含暗色模式、≤600px 响应式、组件样式） |
| `sw.js` | — | Service Worker：`CACHE_VER` + `PRECACHE` 清单 |

### 2.3 题库数据结构

`pwa/data/{2010..2025}.json`，每篇文章：

```
articles[]:
  ├─ aid / title / title_cn / source / topic
  └─ sentences[]:
       ├─ id     例 "2025_text1_s01"
       ├─ para   段落号
       ├─ en     英文原文（照抄真题）
       ├─ cn     官方解析译文（本项目最终统一来源）
       └─ words[]  逐句批注词/词组（w + meaning [+ phonetic]）
  └─ questions[]:
       ├─ id / number / qtype / stem / stem_cn
       ├─ options / options_cn / answer
       ├─ explanation（解析）
       └─ related_sentences[]（定位原文依据）
```

`pwa/data/index.json` 为年份/篇目清单（counts 与年份文件严格一致，构建后校验）。

---

## 三、目录结构

```
english-reading/
├── CHANGELOG.md              # 逐版本更新日志（en2-v2 → en2-v27）
├── 开发全流程总结.md          # 本文档
├── start.bat                 # 本地启动脚本（起静态服务器）
├── make_icons.py             # 生成 PWA 图标
├── .gitignore
├── pwa/                      # ★ 站点根（GitHub Pages 部署目录）
│   ├── index / article / study / vocab / favorites .html
│   ├── manifest.webmanifest
│   ├── sw.js
│   ├── css/style.css
│   ├── js/  common · article · study · storage · dict · annotate .js
│   ├── icons/  favicon.svg · icon-192.png · icon-512.png
│   └── data/
│       ├── 2010.json … 2025.json      # 16 年真题
│       ├── index.json                 # 年份清单
│       ├── dict.json                  # 离线词典（1731 难词）
│       ├── phrases.json               # 真题词组库
│       ├── freq.json                  # 真题考频索引（6473 键）
│       └── deck_*.json                # 内置词书（核心/考纲/形近/词组/唐迟/高频）
└── tools/                    # 数据生产工具链（离线，不进运行时）
    ├── extract_pdf.py         # PyMuPDF 提取真题/解析 PDF 文本
    ├── extract_answers.py     # 抽取答案
    ├── ocr_notes.py / ocr_kb.py  # 扫描图 OCR（RapidOCR）
    ├── build_dict.py          # 从 ECDICT 构建离线词典
    ├── build_phrases.py       # 构建真题词组库
    ├── build_decks.py / build_tc_decks.py  # 构建内置词书
    ├── build_freq.py          # 构建真题考频索引
    ├── clean_*.py / fix_words.py / strip_phrase_prefix.py  # 数据清洗
    ├── validate.py            # 全量校验（JSON 合法性 + index 一致性）
    ├── ecdict.csv             # ECDICT 词库源
    └── extracted/             # PDF 提取/OCR 中间产物
```

---

## 四、开发全流程（按里程碑演进）

整个项目由 Service Worker 缓存版本 `en2-vN` 标记迭代，下面按时间线还原关键决策与实现。

### 阶段 0 · 推倒重来：数据来源纠偏（en2-v2）

**背景**：最初的 2022–2024 题库由 AI 编造，原文/题目/答案均有误，经用户确认**全部作废**。这是项目的转折点，确立了此后一条铁律——**所有内容必须源自真题与官方解析 PDF，绝不 AI 编造**。

- 删除全部 AI 编造 JSON。
- 首批用真题 PDF 重做 **2010–2014 五年**（20 篇）：英文照抄真题、答案以官方解析为准、解析提炼含定位依据、中文参考解析逐句对齐。
- 新增 `tools/extract_pdf.py`（PyMuPDF）按年份提取真题/解析 PDF 全文到 `tools/extracted/`。
- **界面对标精读类 App**：逐句「点击查看翻译」占位条、顶栏「全文翻译」批量开关、右侧题目面板可收缩（状态存 localStorage）。

### 阶段 1 · 交互打磨（en2-v3）

- 定位黄框可再点取消；题目译文纳入「全文翻译」开关统一控制；占位条图标 ☝→▾。

### 阶段 2 · 题库规模化扩充（en2-v5 → en2-v6）

- **en2-v5**：补 **2015–2022 八年**（32 篇），逐句校对、修复 OCR 噪点（如 `agricultoal→agricultural`）。至此 13 年 52 篇 260 题。此时判定 2023–2025 为扫描版 PDF，暂缓。
- **en2-v6**：攻克扫描版——2023/2024 解析、2025 真题与解析经 **OCR 识别 + 逐句校对**补齐，达 **2010–2025 全 16 年、64 篇、320 题**。同批新增**背单词模块**（`study.html`）：SM-2 简化间隔重复、3D 翻面卡、发音、快捷键；首页封面改英文标题避免剧透。

### 阶段 3 · 离线词典与响应式（en2-v7）

- 新增 `tools/build_dict.py` 从 ECDICT 抽取难词，产出 `dict.json` + `js/dict.js`（词形还原查询）。正文**任意难词可点查**，不再局限预标注词。
- 顶栏「高亮难词」开关；背单词卡由 3D 翻面改为**点击原位展开**（移动端更稳）；做题「清除重做」；全站 **≤600px 响应式**断点。

### 阶段 4 · 多词书生词本系统（en2-v8）

- 数据模型升级：`vocab` 记录加 `decks: string[]`，一个词可归属多本词书；SRS 进度按词全局共享。
- 内置「我的生词本」不可删；历史生词一次性迁移补 `decks`。
- 精读页「一键记录难词」；背单词页「按词书背」；生词本升级为**单词本**（词书标签/新建/重命名/删除/导入）。

### 阶段 5 · 词组识别 + 内置词书发布（en2-v9）

- `tools/build_phrases.py` 从真题词组 PDF（坐标分列重建、释义照抄）+ 16 年题库人工词组，产出 `phrases.json`。
- `article.js` 新增 `annotatePhrases()`：最长优先词组匹配，命中包成可点 span。
- `build_decks.py` 产出 3 本考研词表内置词书（真题核心 2230 / 考纲乱序 5551 / 形近易混 1013）+ 真题词组词书。词书**按需导入**，不在启动时灌库。
- 难度判定校准：考研重点词即便高频也保留为难词。

### 阶段 6 · 背单词系统整体升级（en2-v10 → en2-v12）

- **en2-v10**：背单词卡改版为**固定结构**（墨墨/扇贝式），彻底消除翻卡时的高度/按钮抖动（亚像素级恒定）。
- **en2-v11**：**回退**（防误点，快照恢复 SRS 与队列）+ **每日计划**（可设新词量）+ **单词收藏**。
- **en2-v12**：一次性大升级——
  - A. 单词本**分页渲染**（每次 60 张，根治大词书卡顿）；
  - B. 背单词**三模式**（复习优先/只复习/只学新词）；
  - C. **进度续存**（会话快照，跨页返回不归零）；
  - D. **学习统计**（共/新/学习中/已掌握/今日待复习）；
  - E. **真题考频**（`build_freq.py` 产出 `freq.json`，弹卡/背词卡/词卡显示「出现 N 次（M 篇）」）；
  - F. 文章全部词可点、仅生词本高亮、普通词纯文本化；
  - G. 记忆算法加**学习步状态机**（new→learning→review），新词不会一次跳到长间隔。

### 阶段 7 · 词书扩展与发音体验（en2-v13 → en2-v15）

- **en2-v13**：OCR 自动流水线（`ocr_kb.py` + `build_tc_decks.py`）——扫描版词库 PDF 按「绿色词条/黑色正文/真题出处」版面结构解析，产出**唐迟·20 年高分词组 / 熟词僻义**两本内置词书。
- **en2-v14**：单词字体现代化；`speak()` 重写为**有道 dictvoice 在线自然美音优先 + TTS 回退**。
- **en2-v15**：背单词可选**自动读词 / 自动读例句**。

### 阶段 8 · 精读交互与词书体验细化（en2-v16 → en2-v18）

- **en2-v16**：简单词改纯文本（减少空弹卡）；导航栏加「记单词」直达背诵页。
- **en2-v17**：导入词书前先展示**格式引导面板**（字段说明 + 完整模板 + 一键下载模板文件）。
- **en2-v18**：生词记录后**不再自动高亮**正文同词。

### 阶段 9 · 批注系统（en2-v19）

- 新增 `js/annotate.js`（移植自数学笔记）：选中正文即弹浮动条，可打 **3 色荧光 / 整句块高亮 / 文字批注**（批注框支持 Ctrl+V **贴图**）。以 `data-sid` 为锚点、文章 `AID` 分桶，存 localStorage + IndexedDB。导航栏支持**导出/导入标注**（JSON 含贴图 base64）。

### 阶段 10 · 背单词工具栏瘦身与收藏统一（en2-v20 → en2-v21）

- **en2-v20**：**自定义快捷键**悬浮设置窗（5 个动作可改键位，存 localStorage）；**收藏语义统一**——★ 不再是独立星标，而是「是否在收藏目标生词本」，移空回退内置词书、绝不删记录。
- **en2-v21**：背单词工具栏瘦身（选项全收进设置窗、删「重开」）；单词本新增「**重学本书**」（清空该词书 SRS 从头背，二次确认）。

### 阶段 11 · 逐句译文全量改用官方译文（en2-v22 → en2-v24）★本轮收尾

这是本轮开发的重头戏：把全部 16 年真题的逐句 `cn` 从**此前手写意译**统一替换为**官方解析译文**。仅改 `cn`，`en`/`words`/`para`/`id` 不动。

- **en2-v22**：先聚合 2010–2025 全部逐句批注词，去重产出内置词书「真题高频·较难词」（2587 词）。
- **en2-v23**：**2010–2018 九年**（约 65–81 句/年）——官方解析 PDF 右栏译文为准，逐句对照英文修 OCR 错字、补齐缺句/截断句后写回。
- **en2-v24**：**2019–2025 七年**收尾——
  - 2019–2022 为**文本层双栏**版式；
  - 2023–2025 为**扫描图**版式（无文本层），逐页 `get_pixmap` 渲染 PNG → 人工读图按段落对照英文转录 → 修 OCR 错字（如「讨 85 岁→让 85 岁」「座椒设置→座椅设置」）→ 对齐补齐缺句后写回；
  - 覆盖示例：2024 共 63 句、2025 共 74 句。
  - **至此 2010–2025 全 16 年逐句译文均为官方解析译文。**

> 该阶段自建了一套可复用的临时工具链（页面渲染 `_renderpage.py`、逐句写回 `_write_cn.py`、译文映射 `_map_20XX.json`、英文 dump `_en20XX_tX.txt`），任务完成后已整体归拢到工作区外的 `_english_translation_work/` 目录，不污染仓库。

### 阶段 12 · 浏览器标签页图标（en2-v25）★本轮最后

- 新建矢量 `pwa/icons/favicon.svg`：teal 渐变（`#14b8a6→#0f766e`）圆角底 + 白色「英」字 + 右上角金色上标「2」，与主题色一致、小尺寸清晰。
- 5 个页面 head 补 `<link rel="icon">`（SVG 优先、PNG 降级）；`sw.js` PRECACHE 加入 favicon。

### 阶段 13 · 词典扩充后高亮收敛 + 题目词可点（en2-v26 → en2-v27）★本轮收尾

- **en2-v26**：离线词典从 1731 词扩到 8525 词（并入 4 本内置词书）。
- **en2-v27**：词典扩大后「高亮难词」全量标得太密（一篇约 170 个），故引入**精选难词集合** `data/hardwords.json`（熟词僻义 + 真题较难词，2971 词，`tools/build_hardwords.py` 产出），`show-hard` 只高亮命中该集合的 `.word.hard`，一篇降到几十个。
- 同版本新增**题目词可点翻译**：题干/选项/选项池英文词复用精读管道包成可点 span，点词 `stopPropagation` 只弹卡不答题；`resolveExample()` 让题目词也能带例句入生词本；弹卡加视口底部防溢出。

### 阶段 14 · 生词相关收敛（en2-v28 → en2-v29）★本轮收尾

- **en2-v28**：单词本新增**批量删除**——词卡多选框 + 操作条（今日加入/昨日加入快捷全选、全选、删除选中、退出），按 `added_at` 本地日期分组，与分页渲染兼容。
- **en2-v29**：精翻页「**记录生词**」改用精选难词集合过滤（此前按词典命中，词典扩到 8525 词后把 `and`/`new` 等简单词也收录，一篇 150-180 个 → 降到 17-57 个）；精选集合加载失败时自动回退旧逻辑。
- **en2-v30**：精翻导航栏新增**查词框**（Enter 即查，单词词形还原/词组双通道，复用词卡弹窗可加生词本）；「记单词」按钮改新标签页打开。
- **en2-v31**：学习计时器支持**手动暂停/继续与重置**（5 页导航栏加 ⏸/▶ 与 ↺ 按钮，暂停态高亮，跨页保持）。
- **en2-v32**：查词升级——**中文反查**（输中文找英文词/词组）、**查询历史**（输入框聚焦下拉，可清空）；导航栏收纳夜间/导入导出进 **悬浮设置**（⚙，仿背单词页悬浮窗）；查词 **自定义快捷键**（默认 Alt+C 聚焦，设置面板内可改绑）。
- **en2-v33**：**隐藏文章标题防剧透**——首页卡片只显示题型、精翻导航标题默认隐藏，点击标题处才显示。

---

## 五、数据生产流水线（工具链）

运行时是纯静态的，所有数据都由 `tools/` 下的 Python 工具离线生产。核心流水线：

```
真题/解析 PDF
   │
   ├─(有文本层)→ extract_pdf.py（PyMuPDF 提取文本）──┐
   │                                                  ├─→ 人工校对 → 20XX.json（en/cn/words/questions）
   └─(扫描图)  → 逐页 get_pixmap 渲染 PNG              │
                 ocr_notes.py / RapidOCR 识别 ────────┘
                 + 人工读图转录、修 OCR 错字

ECDICT 词库(ecdict.csv) ─→ build_dict.py ─→ dict.json（离线词典）
真题词组 PDF ───────────→ build_phrases.py ─→ phrases.json + deck_phrases.json
考研词表 ×3 ────────────→ build_decks.py ─→ deck_core/syllabus/confusable.json
扫描词库 PDF ───────────→ ocr_kb.py + build_tc_decks.py ─→ deck_tc_phrases/senses.json
16 年题库 sentences ────→ build_freq.py ─→ freq.json（考频索引，与运行时同口径匹配）
16 年题库 words ────────→（聚合去重）─→ deck_realexam.json

全部 JSON ─→ validate.py（合法性 + index 一致性校验）
```

**关键工程点**：

- **考频同口径**：`build_freq.py` 的单词归一（`dict.json` forms）与词组匹配（`phrases.json` 最长优先非重叠）与运行时 `annotatePhrases` **完全一致**，保证徽标数字准确。
- **扫描图并发渲染倒置**：PDF 缩放渲染曾遇并发/倒置问题，已修复（见项目经验记忆）。
- **宁缺勿错**：词组例句检出跨条残句/拆散数字等污染信号则丢弃例句，仅保留正确的 word+meaning。

---

## 六、部署与发布机制

### 6.1 缓存版本机制（核心）

Service Worker 用 `CACHE_VER` 常量控制客户端换新：

- 每次改动数据或代码，**必须 bump `CACHE_VER`**（如 `en2-v24 → en2-v25`），客户端下次访问自动清旧缓存拉新资源。
- 新增文件须同步加入 `sw.js` 的 `PRECACHE` 清单，保证离线可用。

### 6.2 发布流程

```
改代码/数据 → 更新 CHANGELOG.md（顶部追加版本条目）
            → bump sw.js CACHE_VER（+ 需要时改 PRECACHE）
            → git add / commit / push origin main
            → GitHub Pages 自动更新（pwa/ 为站点根）
```

- **远程**：`github.com/Cyrusssxx/english`，分支 `main`，共 13 次提交。
- **推送网络**：直连 github 常被重置，需走本地代理端口 **7890**：
  `git -c http.proxy=http://127.0.0.1:7890 -c https.proxy=http://127.0.0.1:7890 push origin main`
- **本地预览**：`start.bat` 起静态服务器本地走查。

### 6.3 质量保证

- `tools/validate.py`：全量校验 16 个年份 JSON 合法性 + index.json 一致性（历史多次 0 错误 0 警告）。
- 全部 JS 用 `node --check` 语法校验。
- 关键交互（翻卡零抖动、响应式、词典降级、批注恢复）均有针对性走查。

---

## 七、关键难点与解决方案

| 难点 | 解决方案 |
|---|---|
| AI 编造题库不可信 | 推倒重来，确立「一切源自真题+官方解析 PDF」铁律 |
| 2023–2025 PDF 是扫描图无文本层 | PyMuPDF 逐页渲染 PNG + RapidOCR/人工读图转录 + 逐句对照修错字 |
| 官方译文双栏/交错版式各年不同 | 分版式处理：文本层双栏直接抽取；扫描图按段落 `[N]`+`❶❷❸` 标记人工对齐 |
| 大词书全量渲染卡顿 | 单词本分页渲染（每次 60 张，追加不重绘） |
| 翻卡时卡片高度/按钮抖动 | 固定结构卡片（头/滚动释义区/常驻操作条），亚像素级恒定 |
| 新词一次就被拉到长间隔 | 记忆算法加 new→learning→review 状态机与学习步 |
| 生词本词在所有文章被高亮干扰 | 取消常驻自动高亮，改为仅可点查、按需操作 |
| 考频徽标数字要与正文匹配一致 | 考频构建与运行时用同一套归一/词组匹配口径 |
| 直连 GitHub 推送连接被重置 | 走本地代理端口 7890 推送 |

---

## 八、项目现状与统计

- **版本**：`CACHE_VER = en2-v27`，GitHub 全部提交已上线。
- **题库**：2010–2025 全 16 年、64 篇、320 题，逐句译文**全部为官方解析译文**。
- **词典/词书**：离线词典 1731 难词、真题词组库、考频索引 6473 键、7+ 本内置词书。
- **功能完整度**：精读逐句翻译 · 真题做题判分/定位/重做 · 离线词典点查 · 词组识别 · 多词书生词本 · 间隔重复背单词（三模式/续存/统计/发音/快捷键）· 手动批注（荧光/块高亮/文字/贴图/导入导出）· 完全离线可安装。
- **运行时代码量**：JS 约 2158 行（article 626 / study 524 / storage 411 / annotate 403 / dict 127 / common 67）+ 单一大样式表 + 5 个页面。

### 后续可选方向

- 完形填空 / 新题型（Part B）内容补齐（代码框架已支持）。
- 内置词书继续扩充；批注云同步（当前为本地 + 手动导入导出）。

---

*本文档由开发过程沉淀整理，完整逐版本细节见同目录 `CHANGELOG.md`。*
